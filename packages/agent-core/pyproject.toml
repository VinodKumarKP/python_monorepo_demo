[build-system]
requires = ["setuptools>=61.0"]
build-backend = "setuptools.build_meta"

[project]
name = "agent-core"
version = "0.1.0"
description = "Core agent functionality with namespace"
dependencies = []

[tool.setuptools.packages.find]
where = ["src"]
include = ["agent_core*"]
