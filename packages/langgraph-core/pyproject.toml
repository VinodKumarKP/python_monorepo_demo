[build-system]
requires = ["setuptools>=61.0"]
build-backend = "setuptools.build_meta"

[project]
name = "my_company-langgraph-core"
version = "0.1.0"
description = "Core langgraph functionality with namespace"
dependencies = [
    "my_company-agent-core"
]

[tool.setuptools.packages.find]
where = ["src"]
include = ["my_company*"]

[tool.uv.sources]
my_company-agent-core = { workspace = true }
