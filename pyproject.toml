[project]
name = "python-monorepo-demo"
version = "0.1.0"
description = "A monorepo for agent core packages"
requires-python = ">=3.10"
dependencies = []

[tool.uv]
workspace = { members = ["packages/*"] }
